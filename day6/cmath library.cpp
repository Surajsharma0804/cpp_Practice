#include<iostream>
#include<cmath> // Including the cmath library for mathematical functions
using namespace std;
int main() {
    cout<<sqrt(138) << endl; // Using the sqrt function to calculate the square root of 138
    cout << pow(2, 19)<< endl; // Using the pow function to calculate 2 raised to the power of 19
    cout << round(3.14); // Using the round function to round the number
    return 0;
}